language: node_js
git:
  depth: 3
node_js:
  - "0.10"

notifications:
  email: false

before_install:
  - gem install jekyll:2.4.0 rouge --no-document --no-ri --no-rdoc
  - npm install -g grunt-cli bower
  - bower install
